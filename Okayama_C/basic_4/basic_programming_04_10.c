#include <stdio.h>

int main(void) {
    int vx, vy;


    printf("Enter two integers: ");
    scanf("%d %d\n", &vx, &vy);

    sum = vx + vy;
    product = vx * vy;

    printf("vx / vy = %d\n", vx / vy); 
    printf("vx %% vy = %d\n", vx % vy); 

    return (0);
}
